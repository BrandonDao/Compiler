namespace Program.ExtendedName
{
    let a:i32 = 5;
    let b:i32 = 10;

    fn main()
    {
        while(true)
        {
            let c:i32 = b - a;

            let c:i32 = failByRedefinition;
            let a:bool = failByRedefinition;
        }

        while(false)
        {
            let a:bool = thisIsFine;
        }
    }
}